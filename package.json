{
  "name": "sudoku-purescript",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "spago bundle-module --to output/spago-bundle-module-output.js && esbuild output/spago-bundle-module-output.js --bundle --minify --outfile=public/dist/index.js --global-name=Main",
    "start": "spago build && esbuild ./output/Main/index.js --bundle --serve --servedir=. --outfile=output/index.js --global-name=Main",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "spago build --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.27",
    "purescript": "^0.14.7",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "spago": "^0.20.7"
  }
}
